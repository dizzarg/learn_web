version: '2'

services:
  web:
    image: nginx  
    volumes:
      - ./etc/nginx.conf:/etc/nginx/conf.d/default.conf
      - ./public:/home/box/web/public
      - ./uploads:/home/box/web/uploads
    ports:
      - "80:80"
    environment:
      - NGINX_PORT=80
    